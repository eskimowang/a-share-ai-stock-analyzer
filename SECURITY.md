# Security

Do not commit real API keys, portfolio data, report caches, notification tokens, private keys, or production databases.

If a secret is accidentally committed, rotate it immediately and rewrite repository history before making the repository public.
